# Utils for VSCode

[dsl_lira](./dsl_lira/) - raw extension for vscode, providing syntax highlighting for `.lira` files

Installation:
- manually copy to folder with extensions (e.g. `~/.vscode-oss/extensions/` for VSCodium on linux)
- use command (`ctrl+shift+P`) `Developer: install extension from location`
